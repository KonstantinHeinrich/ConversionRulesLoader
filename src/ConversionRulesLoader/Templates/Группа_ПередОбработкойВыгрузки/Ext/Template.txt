#Область Параметры
#Если Клиент И Сервер Тогда
	Отказ = Ложь; // Булево - Если Истина, то группа свойств не обрабатывается
	Источник = Неопределено; // выгружаемый объект.
	Приемник = Неопределено; // xml-узел формируемого объекта приемника
	ВходящиеДанные = Неопределено; // произвольные вспомогательные данные, переданные правилу конвертации из других
	// правил, например, из правила выгрузки данных или правила конвертации свойств.
	ИсходящиеДанные = Неопределено; // произвольные вспомогательные данные, передаваемые правилам конвертации свойств.
	КоллекцияОбъектов = Неопределено; // Неопределено. Если в теле обработчика определить коллекцию (присвоив данной
	// переменной значение, например, описывающее табличную часть или набор движений, например, типа ТаблицаЗначений),
	// то стандартное получение коллекции выполняться не будет.
	НеЗамещать = Ложь; // Булево. По умолчанию определяется в настройках правила, но может быть изменено в теле
	// обработчика. Если установить значение Истина, то для существующих объектов информационной базы загрузка
	// подчиненных объектов (например, табличной части) выполняться не будет.
	НеОчищать = Ложь; // Булево. Если в теле обработчика данному параметру установить значение Истина, то
	// существующие подчиненные  объекты (например, строки табличной части) удаляться не будут. Параметр используется
	// только при загрузке на платформе 1С:Предприятие 8. Параметр игнорируется в обмене на технологии Библиотеки
	// стандартных подсистем.
	УзелКоллекцииСвойств = Неопределено; // xml-узел коллекции свойств
#КонецЕсли
#КонецОбласти