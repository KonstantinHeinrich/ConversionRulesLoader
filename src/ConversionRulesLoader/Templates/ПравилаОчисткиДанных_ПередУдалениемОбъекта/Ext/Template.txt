#Область Параметры
#Если Клиент И Сервер Тогда
	Правило = Неопределено; // ссылка на правило очистки данных
	Объект = Неопределено; // удаляемый (помечаемый на удаление) объект
	Отказ = Ложь; // Булево. Если установить значение Истина, то объект не удаляется
	УдалитьНепосредственно = Ложь; // Булево. По умолчанию определяется в настройках правила, но может быть изменено
	// для каждого объекта. Если установить значение Истина, то объект будет удален непосредственно, в противном случае,
	// только помечен на удаление.
	ВходящиеДанные = Неопределено; // Произвольный - произвольные вспомогательные данные, инициализированные
	// ПередОбработкой правила очистки в параметре ИсходящиеДанные
	Параметры = Новый Структура(); // Структура, в которой хранятся переменные доступные во всех обработчиках
#КонецЕсли
#КонецОбласти